import { FileValueAccessor } from './file-value-accessor.directive';

describe('FileValueAccessor', () => {
  it('should create an instance', () => {
    const directive = new FileValueAccessor();
    expect(directive).toBeTruthy();
  });
});
